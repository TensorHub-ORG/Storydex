{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Minimal preview capability; no shell, filesystem, or updater permissions are granted.",
  "windows": ["main"],
  "permissions": ["core:default"]
}

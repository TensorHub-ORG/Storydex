[package]
name = "storydex-tauri-preview"
version = "0.1.0"
description = "Isolated Storydex Tauri 2 preview shell"
authors = ["Storydex"]
edition = "2021"
rust-version = "1.85"
license = "MIT"
build = "build.rs"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
serde = { version = "1", features = ["derive"] }
tauri = { version = "2", features = [] }
